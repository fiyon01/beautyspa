<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Beauty Spa - Employee Registration</title>
  <!-- Axios CDN-->
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Google Fonts for a more polished look -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <!-- Font Awesome CDN for icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

</head>
<body class="bg-gray-50 font-roboto flex items-center justify-center min-h-screen">

  <!-- Registration Form Container -->
  <div class="bg-white p-8 rounded-lg shadow-xl w-full max-w-sm sm:max-w-md md:max-w-lg transition-transform transform hover:scale-105">
    <div class="text-center mb-6">
      <img src="https://via.placeholder.com/150" alt="Beauty Spa Logo" class="mx-auto mb-4 w-20 h-20 object-cover rounded-full">
      <h2 class="text-3xl font-bold text-gray-800">Employee Registration</h2>
      <p class="text-gray-600 text-sm">Please fill in the form to create your account.</p>
    </div>

    <!-- Registration Form -->
    <form class="space-y-6" id="registration-form">
      <!-- Full Name Field -->
      <div>
        <label for="fullname" class="block text-sm font-medium text-gray-700">Full Name</label>
        <input type="text" id="fullname" name="fullname" required class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-300" placeholder="Enter your full name">
      </div>

      <!-- Email Field -->
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
        <input type="email" id="email" name="email" required class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-300" placeholder="Enter your company email (e.g., employee@beautyspa.com)">
      </div>

      <!-- Password Field -->
      <div>
        <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
        <input type="password" id="password" name="password" required class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-300" placeholder="Create a password">
      </div>

      <!-- Submit Button -->
      <div>
        <button type="submit" class="w-full py-3 bg-indigo-600 text-white font-semibold rounded-md hover:bg-indigo-700 transition duration-300 transform hover:scale-105">
          <i class="fas fa-user-plus mr-2"></i>Register
        </button>
      </div>
    </form>

    <div class="text-center mt-6">
      <p class="text-sm text-gray-600">Already have an account? <a href="/employee/login" class="text-indigo-600 hover:underline">Login</a></p>
    </div>
<!-- Success Modal -->
<div id="successModal" class="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50 hidden">
  <div class="bg-white p-8 rounded-lg shadow-xl w-80 sm:max-w-md">
    <div class="flex justify-center">
      <i class="fas fa-check-circle text-green-600 text-4xl animate-bounce"></i>
    </div>
    <h3 class="text-xl font-semibold text-green-600 mt-4">Registration Successful!</h3>
    <p class="text-gray-700 mt-2">
      Your account has been successfully created. Please check your email for verification instructions to activate your account.
    </p>
  </div>
</div>

      
    <!-- Error Modal -->
    <div id="errorModal" class="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50 hidden">
      <div class="bg-white p-8 rounded-lg shadow-xl w-80 sm:max-w-md">
        <div class="flex justify-center">
          <i class="fas fa-times-circle text-red-600 text-4xl animate-shake"></i>
        </div>
        <h3 class="text-xl font-semibold text-red-600 mt-4">Invalid Email Address</h3>
        <p class="text-gray-700 mt-2">Please use a valid Beauty Spa email address (e.g., employee@gmail.com).</p>
        <div class="mt-4 flex justify-center">
          <button id="closeErrorButton" class="px-6 py-2 bg-red-600 text-white font-semibold rounded-md hover:bg-red-700 transition duration-300">Close</button>
        </div>
      </div>
    </div>

    <!-- Email Verification Success Modal -->
    <div id="emailVerificationSuccessModal" class="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50 hidden">
      <div class="bg-white p-8 rounded-lg shadow-xl w-80 sm:max-w-md">
        <div class="flex justify-center">
          <i class="fas fa-check-circle text-green-600 text-4xl animate-bounce"></i>
        </div>
        <h3 class="text-xl font-semibold text-green-600 mt-4">Email Verified Successfully!</h3>
        <p id="successMessage" class="text-gray-700 mt-2">Your email has been successfully verified. You will be redirected to the login page shortly.</p>
      </div>
    </div>

  </div>

  <script src="/js/script.js"></script>
</body>
</html>
